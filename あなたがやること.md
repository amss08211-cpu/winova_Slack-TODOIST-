# Slack-Todoist連携：あなたがやること

コードは用意済みです。以下を**あなた**が実施すると連携が動きます。

---

## チェックリスト

### 1. Todoist の API トークンを取得する
- [ ] [Todoist](https://todoist.com) にログイン
- [ ] **設定** → **インテグレーション** → **API トークン** をコピー
- [ ] このトークンは後で `.env` に貼る

### 2. 環境変数ファイルを作る
- [ ] このフォルダ（`Slack-Todoist連携`）に `.env` ファイルを**新規作成**
- [ ] 中身を次のように書く（`xxx` の部分を自分の値に置き換える）:

```
TODOIST_TOKEN=ここにTodoistのAPIトークンを貼る
SLACK_SIGNING_SECRET=ここにSlackの「署名シークレット」を貼る
PORT=3000
```

- [ ] Slack の **署名シークレット** は、Slack アプリの **基本情報** → **アプリの認証情報** の「署名シークレット」の「見せる」で表示してコピー

### 3. 依存関係を入れてサーバーを起動する
- [ ] ターミナルでこのフォルダに移動: `cd "Slack-Todoist連携"`
- [ ] `npm install`
- [ ] `npm start` でサーバー起動（起動したままにする）

### 4. ローカルを外から見えるようにする（ngrok）
Slack は「インターネットから届く URL」にしかリクエストを送れません。

- [ ] [ngrok](https://ngrok.com) に登録して ngrok をインストール
- [ ] 別ターミナルで: `ngrok http 3000`
- [ ] 表示された **https の URL**（例: `https://xxxx.ngrok.io`）をコピー

### 5. Slack にスラッシュコマンドを登録する
- [ ] [Slack API](https://api.slack.com/apps) で自分のアプリを開く
- [ ] 左メニュー **「スラッシュコマンド」** を開く
- [ ] **「新しいコマンドを作成」**
  - **コマンド**: `/todoist`（または `/task` など好きな名前）
  - **リクエスト URL**: `https://あなたのngrokのURL/slack/command`  
    例: `https://xxxx.ngrok.io/slack/command`
  - **説明**: 「Todoistにタスクを追加」
  - **使い方のヒント**: `タスク内容 期日 担当 #プロジェクト`（例: `資料作成 明日 浦本 #逆転転職`）
- [ ] **保存**

**重要:** 期日を書かないとタスク化されません。例: `明日` `来週金曜` `2/10`
担当は「中村」のように短く書くと `config/owners.json` の「中村」→「中村俊輔」に変換されます。

### 6. アプリをワークスペースにインストールする（まだなら）
- [ ] Slack アプリの **「アプリをインストール」** からワークスペースにインストール
- [ ] 必要な権限を許可する

---

## 動作確認

1. サーバーが `npm start` で起動している
2. ngrok が `ngrok http 3000` で動いている
3. Slack で `/todoist テストタスク 明日` と送信（期日必須）
4. 「Todoistに追加しています…」→「Todoistにタスクを追加しました」と表示され、Todoist にタスクが増えていれば OK

**担当エイリアス:** `config/owners.json` の `ownerAliases` で「中村」→「中村俊輔」のように短い名前を登録すると、Slack で「中村」と書くだけで担当が振られます。

**進行止まりリマインド:** 期日過ぎタスクを Slack で担当者にメンションして知らせたい場合:
- [ ] `.env` に `SLACK_BOT_TOKEN`（Bot User OAuth Token, xoxb-...）と `SLACK_REMINDER_CHANNEL`（チャンネルID）を追加
- [ ] `config/owners.json` の `ownerToSlackId` に「中村俊輔」→「U01234ABCD」のように Slack のユーザーID を登録
- [ ] 毎日などで `npm run reminder` を実行（cron 例: `0 9 * * * cd /path/to/Slack-Todoist連携 && node run-reminder.js`）

---

## うまくいかないとき

- **「Invalid signature」** → `.env` の `SLACK_SIGNING_SECRET` が正しいか確認（見せる→コピーし直し）
- **「TODOIST_TOKEN が設定されていません」** → `.env` に `TODOIST_TOKEN` を書いたか、サーバーを再起動したか確認
- **Slack で「操作がタイムアウトしました」** → ngrok が動いているか、Request URL が `https://xxxx.ngrok.io/slack/command` になっているか確認

---

進行できなくなったら、このチェックリストの「まだやっていない項目」から再開してください。
