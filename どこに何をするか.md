# どこに・何をすればいいか（一覧）

フォルダはすべて **`マーケ/Slack-Todoist連携`** のなかです。

---

## 必須（Slack → Todoist でタスク追加するまで）

| どこ | 何をする |
|------|----------|
| **`.env`**（このフォルダに**新規作成**） | 次の3行を書く（`xxx` を自分の値に置き換える）:<br>`TODOIST_TOKEN=xxx`<br>`SLACK_SIGNING_SECRET=xxx`<br>`PORT=3000` |
| **Todoist**（ブラウザ） | 設定 → インテグレーション → **API トークン**をコピー → 上記 `TODOIST_TOKEN=` の右に貼る |
| **Slack API**（api.slack.com） | 自分のアプリ → 基本情報 → **署名シークレット**を「見せる」→ コピー → 上記 `SLACK_SIGNING_SECRET=` の右に貼る |
| **ターミナル** | `cd "Slack-Todoist連携"` → `npm install` → `npm start`（起動したままにする） |
| **ngrok**（別ターミナル） | `ngrok http 3000` を実行 → 表示された **https の URL** をコピー |
| **Slack API**（api.slack.com） | スラッシュコマンド → 新規作成 → **リクエスト URL** に `https://(ngrokのURL)/slack/command` を入れて保存 |

ここまでで、Slack で `/todoist なんとか 明日` と打つと Todoist にタスクが入ります（期日必須）。

---

## 担当を「中村」で通したいとき

| どこ | 何をする |
|------|----------|
| **`config/owners.json`** | `ownerAliases` の `"中村": "中村俊輔"` を実名に合わせて編集（または追加）。Slack で「中村」と書くと「担当: 中村俊輔」としてタスクに乗る。 |

---

## 期日過ぎタスクを Slack でメンションリマインドしたいとき

| どこ | 何をする |
|------|----------|
| **`.env`** | 次の2行を**追加**:<br>`SLACK_BOT_TOKEN=xoxb-...`（Slack アプリの Bot User OAuth Token）<br>`SLACK_REMINDER_CHANNEL=C01234...`（投稿したいチャンネルID） |
| **`config/owners.json`** | `ownerToSlackId` の `"中村俊輔": "U01234ABCD"` を、**正式名 → Slack のユーザーID（U で始まる）** に書き換え・追加。 |
| **実行** | 手動: `npm run reminder`。毎日やりたい: cron で `0 9 * * * cd (このフォルダのフルパス) && node run-reminder.js` |

---

## まとめ

- **まずやること:** `.env` を1つ作って、Todoist トークンと Slack 署名シークレットを書く → `npm start` → ngrok → Slack にリクエスト URL を登録。
- **担当エイリアス:** `config/owners.json` の `ownerAliases` を編集。
- **リマインド:** `.env` に Bot トークンとチャンネルID、`config/owners.json` に `ownerToSlackId` を書いてから `npm run reminder`。

細かい手順は **`あなたがやること.md`** に書いてあります。
