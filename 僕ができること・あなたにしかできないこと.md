# 僕（AI）ができること / あなたにしかできないこと

あなたのTODOを最小限にするために整理しました。

---

## 僕ができること（情報をもらえばやる）

以下を**チャットで教えてもらえれば、僕がファイルを編集します**。あなたは確認するだけでOKです。

| 教えてほしいこと | 僕がやること |
|------------------|--------------|
| **担当の対応表**（例: 中村→中村俊輔、田中→田中太郎） | `config/owners.json` の `ownerAliases` を書く・更新する |
| **リマインド用のSlack ID**（例: 中村俊輔→U01234ABCD） | `config/owners.json` の `ownerToSlackId` を書く・更新する |

**例で送ってくれてOK:**  
「担当は 中村→中村俊輔、佐藤→佐藤花子。リマインドでメンションするSlack IDは 中村俊輔=U01ABC、佐藤花子=U02DEF」

※ Slack のユーザーID（Uで始まる）は、Slack のプロフィールを開いて「その他」→「メンバーIDをコピー」で取れます。

---

## あなたにしかできないこと（最小リスト）

ここだけは**ログイン・実行・コピペ**がいるので、あなたがやる必要があります。できるだけ短くしています。

### 1. 2つの値を取って .env に貼る（1回だけ）

| やること | どこで取るか |
|----------|--------------|
| Todoist の API トークン | Todoist（ブラウザ）→ 設定 → インテグレーション → API トークン → コピー |
| Slack の署名シークレット | [api.slack.com](https://api.slack.com/apps) → 自分のアプリ → 基本情報 → 署名シークレット「見せる」→ コピー |

**貼り先:** `Slack-Todoist連携` フォルダに `.env` を新規作成し、中身を次の2行にする（xxx をコピーした値に置き換え）。

```
TODOIST_TOKEN=ここにTodoistのトークン
SLACK_SIGNING_SECRET=ここにSlackの署名シークレット
```

### 2. サーバーと ngrok を起動する（使うたび）

- ターミナル1: `cd "Slack-Todoist連携"` → `npm install`（初回だけ）→ `npm start`
- ターミナル2: `ngrok http 3000` → 表示された **https の URL** をコピー

### 3. Slack に URL を1回登録する

- [api.slack.com](https://api.slack.com/apps) → 自分のアプリ → スラッシュコマンド → 編集
- **リクエスト URL** に `https://(さっきコピーしたngrokのURL)/slack/command` を入れて保存

---

## リマインド（期日過ぎタスクのメンション）も使う場合

- **Slack Bot Token**（xoxb-...）と **投稿先チャンネルID**（Cで始まる）を取得 → `.env` に `SLACK_BOT_TOKEN=` と `SLACK_REMINDER_CHANNEL=` を追加
- 上記「情報をもらえば僕がやること」で、**リマインド用のSlack ID**（正式名→Uxxxx）を教えてもらえれば、`ownerToSlackId` を僕が書く
- 実行はあなた: `npm run reminder`（手動）か、cron で定期実行

---

## まとめ

- **担当対応表やSlack ID** → 教えてもらえれば **僕が config を書く**。あなたのTODOは「情報を送る」だけ。
- **トークン取得と .env への貼り・サーバー起動・ngrok・Slack の URL 登録** → ログインと実行が必要なので **あなたがやる**。ただし「2つの値をコピーして .env に貼る」「2つのコマンドを打つ」「URLを1回登録」だけに絞れている。

何か情報を教えたら僕にできることがあれば、チャットで「〇〇と〇〇を教えて」と聞くので、それに答えてくれればあとは僕がファイルを用意します。
